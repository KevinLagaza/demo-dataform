const project_id = `The id of the project`;
const num = `a random number`;

const columns = {
    user_id :`A unique identifier for a user`,
    age : `The age of a user`
}

module_export = {
    project_id,
    num,
    columns
};
